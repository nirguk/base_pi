node_modules/
.devcontainer-lock.json
.DS_Store
*.log

# pi-llm-debugging output (provider request/response payloads)
pi-llm-debugging/

# pi-web-access runtime cache
.pi/pi/web-search-cache/
# pi-web-access provider config (local: contains container-specific base URL/SSRF)
.pi/web-search.json
.pi/pulse/
.pi/sessions
.pi/web-search-cache
.pi/bin
.pi/auth.json
.pi/models-store.json
.pi/run-history.jsonl
.pi/missions
.pi/projects.json

# runtime symlinks created by .devcontainer/setup.sh (tracked placeholder dirs
# stay in the index; the symlinks themselves are container-local artifacts)
.pi/npm
.pi/tmp/

# llm debug payloads (runtime, never commit)
pi_perf/llm-debug.sqlite
*.sqlite

# uv-managed Python tooling for the pi agent harness (see .devcontainer/
# setup.sh — the venv lives at /opt/base-pi-venv; .venv is a symlink to it).
[project]
name = "base-pi-harness"
version = "0.1.0"
description = "Shared Python tooling for the base_pi agent harness (fuzzy code edits via harnesskit)"
requires-python = ">=3.12"
dependencies = []